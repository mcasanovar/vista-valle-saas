## 1. Herramienta de migración

- [x] 1.1 Agregar un índice sobre `reservations(check_in, check_out)` en `src/persistence/schema.ts`, generar la migración correspondiente con `db:generate` y verificar que `db:check` pasa en offline
- [x] 1.2 Configurar `drizzle.config.ts` para leer las credenciales de conexión desde variables de entorno y agregar el script `db:migrate` en `package.json`; verificar que `db:generate` y `db:check` siguen funcionando sin credenciales reales
- [x] 1.3 Verificar que `db:migrate` es idempotente: ejecutado dos veces seguidas contra el proyecto Supabase real (por decisión explícita del usuario, no había Postgres local/Docker disponible); la segunda ejecución no falló ni duplicó objetos (1 fila en `drizzle.__drizzle_migrations`, 17 tablas)

## 2. Cobertura de integración sobre el historial completo

- [x] 2.1 Extender `scripts/run-postgres-integration.mjs` para reproducir, en orden, todas las migraciones existentes en `drizzle/` (leyendo `drizzle/meta/_journal.json` en vez de un nombre de archivo fijo)
- [x] 2.2 Verificado manualmente por el usuario contra una base de prueba dedicada (`npm run test:integration:postgres` con `VISTA_VALLE_POSTGRES_INTEGRATION_URL` apuntando a una base `_test`/`_integration`, nunca a la base real de Vista Valle).

## 3. Esquema en el proyecto Supabase real

- [x] 3.1 Ejecutar `db:migrate` una vez contra el proyecto Supabase real (`ljwukpcwysvahqmrpwjd`) usando el Session pooler, y verificar por consulta directa que las tablas, tipos y restricciones del esquema Drizzle existen en `public` — 17 tablas y el índice de `reservations` confirmados
- [x] 3.2 Confirmar que la aplicación runtime sigue usando el Transaction pooler (`DATABASE_URL` en `.env.local`, puerto 6543) y no la cadena usada para migrar

## 4. RLS y Storage

- [x] 4.1 Aplicar `supabase/rls/operational-tables.sql` contra el proyecto real y verificar por consulta que RLS está habilitado y las políticas existen en las tablas operativas — se corrigió el archivo para incluir `reservation_items`, `company_quotations` y `company_quotation_lines`, que faltaban; las 17 tablas quedaron con `rls=true, force=true`
- [x] 4.2 Aplicar `supabase/storage/room-images.sql` contra el proyecto real y verificar que el bucket de imágenes de habitaciones existe con las reglas de acceso definidas — se quitó el `ALTER TABLE storage.objects ENABLE ROW LEVEL SECURITY` (bloqueado por permisos de Supabase; ya viene habilitado por defecto); bucket y política de lectura pública confirmados

## 5. Administrador productivo

- [x] 5.1 Crear el usuario `vistavallespa@gmail.com` en Supabase Auth del proyecto real vía `inviteUserByEmail` (envía correo para definir contraseña) y configurar `ADMIN_ALLOWED_EMAILS=vistavallespa@gmail.com` en `.env.local`. **Pendiente fuera de mi alcance:** deshabilitar "Allow new users to sign up" en el dashboard (Authentication → Providers → Email) — no hay CLI/Management API disponible aquí para hacerlo remotamente; `supabase/config.toml` lo declara pero no está vinculado al proyecto real
- [x] 5.2 Verificado: test unitario dedicado (`tests/authorization.test.ts`) confirma que solo `vistavallespa@gmail.com` autenticado es reconocido como administrador; confirmado además que el usuario real existe en Supabase Auth (único usuario, invitación enviada, pendiente de confirmar)

## 6. Contenido real de habitaciones

- [x] 6.1 Escribir un script server-only idempotente (upsert por slug) que suba las fotografías reales al bucket de Storage y cargue los datos reales de las tres habitaciones en la tabla `rooms`, dejándolas `active=false` hasta aprobación — `scripts/load-room-content.mjs` + plantilla `scripts/room-content/rooms.example.json`
- [x] 6.2 Ejecutado contra el proyecto real con el contenido de `proposes/information/rooms.js` (convertido a `scripts/room-content/rooms.json`, usando las fotografías ya existentes en `public/rooms/`). Verificado por consulta: 3 habitaciones cargadas con `active=false`, capacidad/camas/precio correctos, 1 imagen c/u en Storage (7-8 amenities enlazadas c/u), imagen pública accesible (200 OK). Nota: 1 foto por habitación, no 3 — el usuario confirmó seguir así por ahora; se pueden agregar más después con el mismo script.

## 7. Verificación final

- [x] 7.1 Verificado sin modificar `VISTA_VALLE_CONFIG_CONTEXT` de desarrollo ni activar `BOOKING_ENABLED`: Postgres (cliente Drizzle real contra el proyecto, `select from rooms` exitoso), Auth (usuario admin real creado y listado vía Admin API), Storage (bucket y política confirmados por consulta)
- [x] 7.2 Documentado en `design.md` (nueva sección "Variables de entorno productivas para Vercel"): qué queda resuelto por este cambio, qué depende de gestión aparte, y qué no cambia
- [x] 7.3 Verificación visual completa en navegador (servidor local con `VISTA_VALLE_CONFIG_CONTEXT=production` real, sin tocar `.env.local`): catálogo y detalle de habitaciones reales renderizan correctamente con fotos, precios y servicios. Encontró y corrigió 2 bugs reales:
  - `scripts/load-room-content.mjs` guardaba las rutas de Storage con el *slug* de la habitación; `assertRoomImagePath` (`src/infrastructure/storage/contracts.ts`) exige el *UUID* de la habitación. Corregido el script, resubidas las 3 fotos con la ruta correcta, y eliminados los objetos huérfanos con la ruta vieja.
- [x] 7.4 A pedido del usuario, `.env.local` quedó con `VISTA_VALLE_CONFIG_CONTEXT=production` de forma persistente (ya no solo para verificación puntual). Para que la app arranque, `MERCADO_PAGO_ACCESS_TOKEN`, `MERCADO_PAGO_WEBHOOK_SECRET`, `AI_PROVIDER`, `AI_MODEL`, `AI_API_KEY`, `RESEND_API_KEY` y `ADMIN_NOTIFICATION_EMAIL` se cambiaron de `mock-*`/`*.test` a valores `unset-*` no reales (solo evitan el bloqueo de `assertMockValuesAreExplicitlyAllowed`; Mercado Pago, el asistente IA y el envío de correo no son funcionales). `BOOKING_ENABLED` permanece `false`. Verificado con `npm run dev` normal (sin overrides): catálogo y detalle sirven datos reales de Supabase.
  - `next.config.ts` no tenía `images.remotePatterns`, por lo que `next/image` rechazaba cualquier foto servida desde Supabase Storage. Agregado, derivando el host permitido de `NEXT_PUBLIC_SUPABASE_URL`.

## 8. Repositorio de habitaciones en producción (hallazgo durante el apply)

- [x] 8.1 HALLAZGO CRÍTICO: `getRoomReadSource()` bajo contexto `production` construía la fuente con un arreglo vacío hardcodeado (`src/features/rooms/source.ts`) — el catálogo y detalle público nunca leían la tabla `rooms`, sin importar el contenido cargado en la tarea 6. No estaba en el alcance original de este cambio; se agregó tras confirmarlo con el usuario.
- [x] 8.2 Escribir `src/infrastructure/database/room-source.ts`: repositorio Drizzle que consulta `rooms` (activas), `room_images` (ordenadas por posición) y `room_amenities`/`amenities`, reutilizando `createRoomImageStorage()` para las URLs públicas de Storage
- [x] 8.3 Convertir `getRoomReadSource()` a async y conectar el repositorio bajo `production`; ampliar `isPublishableRoom` (`read-model.ts`) para aceptar imágenes con URL absoluta `https://` además de rutas locales `/...`
- [x] 8.4 Actualizar los ~14 puntos de llamada a `getRoomReadSource()` (páginas, rutas API, servicios de disponibilidad/reservas/notificaciones) para `await` la nueva firma async; `npm run typecheck` limpio
- [x] 8.5 Corregir los tests rotos por este cambio y por la regeneración de la migración (tarea 1.1): `tests/migration.test.ts` (16→17 tablas; nombre de índice desactualizado), `tests/storage.test.ts` (assertion sobre la línea de RLS removida en 4.2), `tests/home-page.test.tsx` (`HomePage` ahora es un Server Component async), `tests/seo.test.ts` (`sitemap()` ahora async). Suite completa: 294/297 pasan; los 2 fallos restantes (`prebooking-review-controller.test.tsx`, `room-detail.test.tsx`) son preexistentes de ediciones externas anteriores a este cambio, no relacionados con Supabase — no se tocaron.

## 9. Retroalimentación de carga en operaciones contra Supabase

- [x] 9.1 Auditado: catálogo/detalle de habitaciones y listados admin (reservas, calendario) cargaban sin ningún indicador (gap real, recién posible de notar porque 8.2-8.4 los volvió async de verdad); disponibilidad ya tenía skeleton propio. Botones de acción contra Supabase ya cubiertos con `loading`: búsqueda de disponibilidad, formulario/controlador de cotización empresa, confirmación de reserva, transición de estado de reserva admin, resumen de reserva. Botones sin `loading` revisados (`Continuar` del formulario de huésped, quitar/agregar habitación de la selección, formulario de demostración de empresa) no llaman a Supabase — son estado local o formularios de demostración sin envío; no requieren cambio. Los formularios admin restantes (bloqueos, sincronizaciones, asistente) usan envío nativo de formulario/navegación de página, no fetch en cliente, por lo que el mecanismo de carga aplicable es `loading.tsx` de ruta, no `Button loading`.
- [x] 9.2 Creado el átomo `Skeleton` en `src/presentation/atoms`; aplicado en `app/habitaciones/loading.tsx` y `app/habitaciones/[slug]/loading.tsx` (nuevos, cierran el gap real), refactorizados `app/disponibilidad/loading.tsx` y `results.tsx` para usarlo también, y agregado `app/(admin-protected)/admin/reservas/loading.tsx` y `.../calendario/loading.tsx`
- [x] 9.3 No se encontraron botones de acción contra Supabase sin `loading`; todos los existentes ya lo usaban únicamente en el propio control (verificado, ninguno bloquea pantalla completa)
- [x] 9.4 Agregado `tests/loading-states.test.tsx` (5 tests): átomo `Skeleton`, y las 5 superficies con `loading.tsx` nuevas/actualizadas, verificando región `aria-busy` + texto de estado. Suite completa: 299/302 pasan (los 2 fallos restantes son preexistentes, no relacionados)

## 10. Repositorio de disponibilidad en producción (hallazgo posterior al cierre, reportado por el usuario)

- [x] 10.1 HALLAZGO: al activar `VISTA_VALLE_CONFIG_CONTEXT=production` de forma persistente, la búsqueda de disponibilidad fallaba siempre con "No pudimos consultar disponibilidad". Causa: `getAvailabilitySearchRepository()` (`src/features/availability/search-source.ts`) lanzaba `AvailabilitySearchSourceUnavailableError` para cualquier contexto distinto de `mock` — el mismo patrón de fail-closed ya encontrado con las habitaciones (tarea 8), pero para disponibilidad.
- [x] 10.2 Extraída y exportada `listOccupyingIntervals` desde `src/infrastructure/database/room-lock.ts` (antes privada, usada solo dentro de la transacción de bloqueo) con un tipo `QueryableDatabase` que acepta tanto `ProductionDatabase` como la transacción, para no duplicar la consulta de reservas confirmadas + holds vigentes + bloqueos activos.
- [x] 10.3 Creado `src/infrastructure/database/availability-source.ts` (`createDrizzleAvailabilityRepository`), que reutiliza esa misma función. `getAvailabilitySearchRepository()` ahora usa `createDatabaseBoundary()` para elegir el repositorio real en producción; `createAvailabilitySearchRepository("production")` se dejó intacto (sigue lanzando el error, cubierto por su test existente) para no romper ese contrato ya probado.
- [x] 10.4 Verificado: `typecheck`/`lint` limpios, suite completa 299/302 (mismos 2 fallos preexistentes), y verificación en vivo — `/api/availability` y `/disponibilidad` devuelven las 3 habitaciones reales sin conflictos para fechas futuras.

## 11. Escritura real de reservas en producción (hallazgo posterior al cierre, reportado por el usuario)

- [x] 11.1 HALLAZGO: al intentar confirmar una reserva real, la API respondía 503 "No pudimos confirmar la reserva" sin persistir nada. Dos causas independientes, ambas fail-closed deliberados nunca cerrados para producción:
  - `getPayAtPropertyBookingConfirmationService()` (`src/features/reservations/confirm-pay-at-property.ts`) lanzaba `BookingConfirmationUnavailableError` para cualquier contexto distinto de `mock` (comentario original: "Production has no reservation-writing command wired yet").
  - `getPublicBookingRequestLimiter()` (`src/infrastructure/security/request-limiter.ts`) devolvía `null` en producción a propósito, por seguridad: un limitador en memoria de un solo proceso no protege un despliegue multi-instancia, así que el endpoint prefería fallar cerrado antes que dar una falsa sensación de protección.
- [x] 11.2 Refactorizado `confirmPayAtPropertyBooking` en un núcleo genérico (`confirmPayAtPropertyBookingWith<TContext>`) parametrizado por `guestRepository`/`reservationRepository`/`roomLockGateway`/`notificationOutboxWriter`, con `confirmPayAtPropertyBooking` (mock, usado por tests existentes) como envoltorio que fija las dependencias mock. `createPayAtPropertyBookingConfirmationService("production")` se dejó intacto (sigue lanzando el error, cubierto por su test existente); `getPayAtPropertyBookingConfirmationService()` ahora decide por `createDatabaseBoundary()` y arma las dependencias reales (`createDrizzleRoomLockGateway`, `createDrizzleGuestRepository`, `createDrizzleReservationRepository` — los tres ya existían, escritos pero nunca conectados) cuando el contexto es `production`.
- [x] 11.3 Creado `src/infrastructure/database/booking-confirmation-source.ts` (`getProductionPublicBookingConfirmation`) para que `/reserva/[publicId]` lea la reserva real (reserva + huésped + habitaciones) en vez de devolver `null` siempre en producción.
- [x] 11.4 Por decisión explícita del usuario (ver hallazgo 11.1): `getPublicBookingRequestLimiter()` ahora reutiliza el mismo limitador en memoria en producción, mientras el despliegue siga siendo un solo proceso; documentado en el código que debe reemplazarse por un limitador compartido (Redis/Upstash) antes de desplegar detrás de múltiples instancias serverless.
- [x] 11.5 La tienda de idempotencia (`mockIdempotencyStore`) también se reutiliza tal cual para producción — mismo trade-off de un solo proceso, documentado en el código; revisar antes de escalar a múltiples instancias.
- [x] 11.6 Verificado extremo a extremo en el navegador contra el proyecto real: catálogo → disponibilidad → carrito → datos de huésped → confirmar reserva → `201 Created` con `publicId` real → página `/reserva/[publicId]` mostrando los datos reales leídos de Postgres. `typecheck`/`lint` limpios, suite completa 299/302 (mismos 2 fallos preexistentes). Quedaron 2 reservas de prueba con datos ficticios en la base real (Juan Pérez, Carla Rivas); por decisión del usuario, no se borraron — pendientes de que las revise en el dashboard.

## 12. Interruptor operativo `BOOKING_ENABLED` (a pedido del usuario)

- [x] 12.1 HALLAZGO al documentar 11: `BOOKING_ENABLED` se validaba en `src/config/server.ts` pero ningún código lo leía — no existía forma de pausar reservas reales. Corregido el `design.md` que afirmaba erróneamente lo contrario.
- [x] 12.2 Cambiado el default del esquema de `"false"` a `"true"` (a pedido explícito del usuario: nunca debe bloquear reservas por un valor ausente).
- [x] 12.3 Agregado `isBookingAcceptanceEnabled()` en `confirm-pay-at-property.ts`: solo aplica `BOOKING_ENABLED` bajo contexto `production` (vía `createDatabaseBoundary()`); bajo `mock` siempre devuelve `true`, para no romper `.env.test.example`/`scripts/with-test-env.mjs`, que fijan `BOOKING_ENABLED=false` como aserción de seguridad para toda corrida de tests automatizados — un valor que antes era decorativo y ahora sí importaría si el gate no se hubiera restringido a producción.
- [x] 12.4 Conectado el interruptor en dos puntos: `app/api/bookings/pay-at-property/route.ts` (rechaza con 503 y mensaje claro antes de tocar el limitador/Postgres) y `app/pre-reserva/page.tsx` → `PrebookingReviewController` → `BookingConfirmationController` (oculta el botón de confirmar y muestra un aviso "Reservas directas pausadas" en vez de dejarlo clickeable para luego fallar).
- [x] 12.5 Agregados tests: `tests/booking-acceptance.test.ts` (`isBookingAcceptanceEnabled` respeta el contexto), nuevo caso en `tests/booking-confirmation-controller.test.tsx` (aviso de pausa sin botón). Suite completa: 302/305 pasan (mismos 2 fallos preexistentes + este grupo sumó 3 tests nuevos).
- [x] 12.6 `.env.local` tenía `BOOKING_ENABLED=false` explícito (heredado de cuando la variable era decorativa), lo que habría pausado las reservas reales ya verificadas en la tarea 11. Por decisión del usuario, se quitó la línea para que aplique el nuevo default (`true`); verificado en navegador que el botón de confirmar reserva vuelve a estar activo.
