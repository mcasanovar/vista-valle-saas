## 1. Repositorio de reservas (producción)

- [x] 1.1 En `src/infrastructure/database/reservation-repository.ts`, dentro de `transitionReservationState`, cuando `transition.to === "cancelled"`: seleccionar los `payments` de la reserva con `status !== 'cancelled'` dentro del mismo `tx`. Verificar con un test que trae correctamente los pagos existentes de una reserva de prueba. Tests agregados en `tests/postgres-reservation.integration.test.ts` (gated tras `POSTGRES_RESERVATION_INTEGRATION_ENABLED`; no hay Postgres real disponible en este entorno para ejecutarlos, pero compilan y el archivo colecta/se salta correctamente).
- [x] 1.2 Actualizar cada pago seleccionado a `status: 'cancelled'` e insertar un `auditEvents` por pago (`action: "payment.cancelled"`, `before: { status: <estado previo> }`, `after: { status: "cancelled" }`, `entityType: "payment"`) dentro del mismo `tx` que la actualización de la reserva. Verificar con un test que una reserva con un pago `approved` termina con ese pago en `cancelled` y con el evento de auditoría correspondiente. Test: "cancels the reservation's approved payment and records its prior status in an audit event".
- [x] 1.3 Verificar con un test que una reserva con múltiples pagos (por ejemplo uno `rejected` y uno `approved`) termina con todos en `cancelled`, cada uno con su propio evento de auditoría. Test: "cancels every payment on a reservation with multiple payments, each with its own audit event".
- [x] 1.4 Verificar que si la actualización de un pago falla, la transacción completa revierte (la reserva no queda `cancelled` con pagos a medio actualizar). Verificado por inspección de código: el bloque de cancelación de pagos corre dentro del mismo `tx` que ya envuelve `runLockedMany` en `db.transaction(...)` (`room-lock.ts`), y ningún error se captura/silencia dentro del bloque, así que cualquier fallo en la escritura de un pago propaga y revierte toda la transacción, incluida la actualización de `reservations.status`. No se pudo forzar una falla real sin una base Postgres disponible en este entorno.

## 2. Repositorio mock (contexto de pruebas)

- [x] 2.1 En `src/features/reservations/reservation-repository.ts` (`createMockReservationRepository`), replicar la misma regla sobre el storage en memoria de pagos cuando `transition.to === "cancelled"`. Verificar que los tests que usan el mock repository pasan con la misma expectativa que el adapter de Drizzle. Incluyó ampliar `ApprovedPayNowPayment.status` a `"approved" | "cancelled"` y corregir `getApprovedPaymentsTotalClp` para filtrar el pago pay_now por estado (antes asumía siempre aprobado). Tests nuevos en `tests/reservation-pay-at-property.test.ts` para ambos caminos (pay-at-property y pay-now vía hold) — 86/86 tests relacionados pasan.

## 3. Limpieza de comportamiento documentado

- [x] 3.1 Actualizar el comentario en `src/features/admin/reservation-actions.ts` que dice "Never touches payment state — cancellations do not trigger refunds" para reflejar el nuevo comportamiento.
- [x] 3.2 Revisar `src/features/admin/operational-alerts.ts` y cualquier otro lugar que lea `payments.status` asumiendo que un pago `approved` de una reserva cancelada sigue `approved`, y ajustar si corresponde. Revisado: `operational-alerts.ts`, `admin/dashboard.ts` (mock) y la vista de detalle de reserva se autocorrigen bien (dejan de contar/alertar sobre pagos que ya no están aprobados). Encontré un caso real que sí necesitaba ajuste: la columna de pago del listado de reservas (`admin-reservation-source.ts` + `app/(admin-protected)/admin/reservas/page.tsx`) mostraba "Pendiente" para una reserva cancelada. Confirmado con el usuario y corregido en 3.3.
- [x] 3.3 (agregada durante 3.2, confirmada con el usuario) Agregar un tercer estado `"cancelled"` a `AdminReservationPaymentSummary` (`admin-reservation-source.ts`) para que una reserva cancelada muestre su pago como "Cancelado" en el listado, no "Pendiente" ni "Pagado". Actualizar `page.tsx` (`paymentStatusMeta`) y capturar el requisito nuevo en `specs/reservation-administration/spec.md`.

## 4. Verificación end-to-end

- [x] 4.1 En el panel admin, cancelar una reserva con pago aprobado (pagada) y confirmar en el detalle de la reserva que el pago aparece como `cancelled`. No se pudo probar en vivo en este entorno: tanto `transitionAdminReservationWithResult` como el listado/detalle de reservas (`admin-reservation-source.ts`) requieren explícitamente `boundary.context === "production"` (fallan con "Las reservas solo se pueden operar en producción." / "Las reservas no están disponibles." en el contexto mock usado para desarrollo local, verificado navegando a `/admin/reservas`). Cubierto en su lugar por el test de integración Postgres "cancels the reservation's approved payment and records its prior status in an audit event" (no ejecutable aquí por falta de una base Postgres real, mismo límite que la sección 1) y por los tests del mock repository (sí ejecutados).
- [x] 4.2 Cancelar una reserva con pago pendiente y confirmar que también queda `cancelled`. Mismo límite que 4.1; cubierto por el test de integración "cancels the reservation's approved payment..." (usa un pago `approved` como caso principal) y, para el caso `pending` específicamente, por el test del mock repository "cancels the reservation's pending pay-at-property payment on cancellation" (sí ejecutado, pasa).
- [x] 4.3 Correr la suite de tests existente de reservas y pagos y confirmar que pasa, actualizando los tests que fijaban la expectativa anterior ("el pago se preserva al cancelar"). `npm run typecheck` limpio. `node scripts/with-test-env.mjs npx vitest run` → 811/812 tests pasan (167/168 archivos); el único fallo, `prebooking-review-controller.test.tsx`, es preexistente y no relacionado (no toca reservas, pagos ni los archivos modificados en este change). No encontré ningún test existente que fijara la expectativa anterior de "el pago se preserva al cancelar" (esa regla vivía solo como comentario/spec, sin test que la cubriera).
